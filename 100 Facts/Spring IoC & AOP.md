
| domain | layer   | type  |
| ------ | ------- | ----- |
| #java  | #spring | #fact |

## 0. 为什么会有Spring?
* 之前的Java开发存在痛点，Spring框架能够有效解决
	* IoC-DI解决了什么
		* IoC是一种设计原则，指控制权的转移
		* DI是IoC的一种具体实现
		* 对象、依赖管理复杂，传统是由对象主动控制：对象的创建、对象的生命周期、对象的依赖关系，较为复杂。IoC将以上三者的控制权移交给Spring IoC容器。
		* 实现方式是DI
	* AOP解决了什么？
		* 痛点：业务逻辑被非业务逻辑（日志，权限等）污染
		* 使用AOP处理非业务逻辑，做到职责分离

# Spring IoC
## 定义
* IOC 即控制反转，指的是将对象的控制权 交给了Spring容器，容器负责创建和管理对象，我们只需要接受容器注入的对象
## 优点
- **降低耦合**：在非IOC的传统编程中，对象的创建和依赖管理硬编码在业务逻辑中，与具体实现类强绑定，一旦要更换实现类，就需要改动业务代码。而IOC模式下，在业务类里依赖接口，而非具体实现，如果要更换实现类，只需要在配置类中更换实现类。
- **不需要重复创建相同的对象**，Spring容器中的Bean对象默认是单例的
# Spring AOP
## 定义
* AOP 即面向切面编程，用作无侵入式的代码增强，一般对公共的行为或者逻辑进行抽取，将横切逻辑和业务逻辑分离，提升代码复用性和可维护性。
## 实现方式（[[动态代理]]）
- **JDK动态代理**：基于接口，生成接口的代理类。
- **CGLIB代理**：基于类，通过继承目标类生成子类，重写方法增强。
