
| domain | layer | type  |
| ------ | ----- | ----- |
| #db    |       | #fact |
## 倒排索引

* 是什么
  * 关键词到文档的映射
  * 存储每个关键词以及包含这些关键词的文档信息

### 构建过程

* 分词
  * 将文本内容拆分为独立的单词或短语
* 归一化
  * 转小些
  * 去除停用词：对查询结果影响不大的词 --》“的”
  * 词干提取：归于基本形式 running-〉run
* 创建倒排索引
  * 根据分词结果，为每个词项建立一个到文档的映射关系

### 搜索过程

* 解析查询条件
  * 将查询关键词解析为分词结果
* 查找倒排索引
  * 查找词对应到的文档列表
* 合并结果
  * 根据查询类型合并文档列表，不指定则默认为OR
    * 若为AND，返回两个列表交集
    * OR，并集
* 排序结果
  * 根据相关性评分（TF-IDF），排序返回

### ES分词器

* 组件
  * 字符过滤：预处理如删除html标记等
  * 分词：将文本拆分为一系列的tokens，例如按照空格或标点切分
  * 此项过滤：处理分词结果，如转小写，去停用词，词干提取等
* 常见分词器
  * 默认-standard分词器
    * 多语言，按标准分词规则切分
  * simple分词器
    * 仅按非字母字符切分，且将所有词项转为小写
  * whitespace
    * 空格切分
  * keyword
    * 整个输入作为单独词项
  * stop
    * 在standard基础上，去掉常用的停用词is the...



```
input { # 输入
  jdbc {
    jdbc_driver_library => "/Users/czl/Downloads/logstash-8.9.0/config/mysql/mysql-connector-j-8.3.0.jar"  # MySQL JDBC 驱动路径
    jdbc_driver_class => "com.mysql.cj.jdbc.Driver"  # MySQL JDBC 驱动类
    jdbc_connection_string => "jdbc:mysql://localhost:3306/mockredbook_db"  # MySQL 链接
    jdbc_user => "root"  # MySQL 用户名
    jdbc_password => "785070"  # MySQL 密码
    jdbc_paging_enabled => "true" # 开启分页，分批次查询
    jdbc_page_size => "1000" # 每批查询 1000 条

    # 查询 SQL 脚本路径（从数据库提取数据）
    statement_filepath => "/Users/czl/Downloads/logstash-8.9.0/config/mysql/sync-es-note-index/jdbc.sql"

    # 任务调度频率
    schedule => "* * * * *"  # 每分钟执行一次查询
  }
}

filter {
  # 如果需要进一步处理数据，可以在这里添加其他过滤器

  mutate { # mutate 提供了多种功能来处理和转换字段值
    split => ["img_uris", ","] # 分隔 img_uris 字段为数组
    add_field => { "cover" => "%{[img_uris][0]}" } # 添加一个 cover 字段，并将 img_uris 数组中第一个 url 设置为其值
    remove_field => ["img_uris"]  # 移除 img_uris 字段
    remove_field => ["@timestamp"] # 移除 @timestamp 字段
    remove_field => ["@version"] # 移除 @version 字段
  }
}

output { # 输出
  elasticsearch { # Elasticsearch 配置
    hosts => ["http://localhost:9200"] # Elasticsearch 链接
    index => "note"  # Elasticsearch 索引名称
    document_id => "%{id}"  # 使用查询结果中的 id 字段，作为文档的唯一标识
  }

  stdout {
    codec => json_lines  # 打印日志输出到控制台，便于调试
  }
}


```
