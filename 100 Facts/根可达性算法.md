
| domain | layer | type  |
| ------ | ----- | ----- |
| #java  | #jvm  | #fact |

视作 GC Root 的对象：
 1. 方法区中类的静态字段引用的对象
 2. 本地方法栈（JNI）中的引用对象
 3. JVM 栈（虚拟机栈）中的引用对象
 4. 被同步锁持有的对象
	 1. 当前作为 `synchronized` 监视器锁的对象。