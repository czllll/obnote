## Mysql
* MVCC
	* MVCC 核心思想？
	* 典型应用？
	* 依赖于？
	* 实现原理？
* B+树索引
	* 为什么使用 B+树作为索引的数据结构？
	* B树和B+树的区别？
	* 聚集索引和二级索引？
	* 回表和覆盖索引？
	* 索引失效情况？
* 日志
	* redo
		* wal 是什么？
		* 用途？
	* undo
		* 用途？
		* 保证事务原子性的原理？
	* bin
		* 是什么？
		* 用途？
		* 三种格式？
* 执行 sql 查询语句的过程？
* explain 内容
	* 包含哪些？
	* select_type包含哪些？
	* type 包含哪些？
	* extra 包含哪些？
	* 什么是[[索引条件下推]]？
* sql 优化
	* 慢 sql 定位？
	* 慢 sql 分析，慢的原因在哪里？
		* 两个太大，三个不当，一个不合理一个性能差
	* 优化
		* 索引优化？
			* 4 点，1 点数量，三点建索引限制
		* sql 语句优化？
			* ✳️，小驱动大的，允许重复就用 union all
		* 表结构优化？
			* 避免多表联查两点
			* 数据类型三点
			* NULL 一点，字段数量一点
		* 业务优化？
		* 架构优化？
* InnoDB 和 MyISAM 区别？
	* 默认
	* 适用场景 2 点
	* 主要区别 6 点
* 主从复制
	* 是什么？
	* 主要过程？
	* 复制方式 有哪几种？3 种
* RR 和 RC 区别：RR 是可重复读，RC 是读已提交
	* 什么是快照读
	* MYSQL 的锁类型？
	* binlog 三种格式分别被RC RR如何支持？
		* [[为什么不同隔离级别下对 binlog 模式还有要求？]]？
	* 为什么互联网公司选择使用 RC？
* 查询关键字的执行顺序？分三个阶段
* 聚簇索引和非聚簇索引的区别？

## Redis
* Redis 数据类型？
	* 什么是跳表？
* 分布式锁？
	* SetNx
		* 命令效果？
		* 加锁解锁的实现？
		* 问题？
	* Redission
		* 可重入的原理
		* 超时续约机制（看门狗机制？）
			* 为什么要这个机制？
			* 续期规则？
	* 数据（锁的 key）未同步导致并发问题的解决
		* 多主架构RedLock 红锁方案
	* [[MultiLock和 RedLock]]
* 持久化
	* RDB
		* 是什么？什么命令？
		* 底层原理？
			* 两点 fork+copyonwrite
		* 优缺点？
	* AOF
		* 是什么？
		* 写日志文件频率的三个模式？
		* AOF 文件重写是什么？
		* 优缺点？
	* 混合持久化？
		* 什么版本推出的？以什么为基础？
		* 过程？
		* 文件结构？
		* 优缺点？
* 内存淘汰策略
	* 策略有哪些？TTL/non-TTL/noeviction
	* 选择建议
	* 淘汰流程？
* 缓存击穿
	* 是什么？
	* 解决方案？
* 缓存穿透
	* 是什么？
	* 解决方案？
* 缓存雪崩？
	* 是什么？
	* 解决方案？
* 热 key
	* 是什么？
	* 影响？
	* 解决？
* 大 key
	* 是什么？
	* 影响？
	* 解决？
* redis 高可用
	* redis 主从
		* 为什么需要多节点架构？
		* 为什么要设计成读写分离？
		* 主从架构的问题？
	* redis 哨兵
		* 哨兵机制的作用是什么？
		* 哨兵机制是如何监控的？
		* master 宕机后，哨兵如何选举新的 master？
		* 哨兵模式的问题？
	* redis cluster
		* cluster 的特征？
		* 数据分片的原理？
			* [[CRC16 算法]]
		* 为什么将数据与插槽绑定来实现分布式存储？
		* 当扩容新加入节点时，槽位重分配，槽位信息怎么同步到集群中所有节点？
		* Redis cluster 的优点？