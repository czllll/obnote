#cs61a #python #语法
## 函数
* `isinstance(object, classinfo)`
	* 用于检查一个对象是否是指定类型的实例
### 生成器
* 用于创建迭代器，返回一系列的值，每次调用生成一个值（惰性计算：节省内存）
* 返回使用`yield`而非`return`,每次遇到`yield`函数暂停并返回一个值，而函数的状态
```python
def count_up_to(max_value): 
	count = 1 
	while count <= max_value: 
		yield count 
		count += 1
```
### 纯函数
#### 纯函数
* 特点
	* 无副作用：不改变外部状态
	* 相同输入必有相同输出
#### 非纯函数
* 特点
	* 有副作用
	* 输出不稳定：输出不仅仅以来输入参数，还可能依赖外部状态。相同的输入在不同环境下可能有不同的返回结果
### 闭包（Closure）
#### 是什么
* 闭包由外部函数、内部函数、外部函数的局部变量组成
* 当inner引用outer的局部变量时，即使outer已经执行完毕，inner仍可以访问那些变量
#### 基本结构
```python
def outer_function(msg):
    def inner_function():
        print(msg)
    return inner_function
closure = outer_function("Hello, World!")
closure()  # 输出: Hello, World!
```
* **注意**：outer返回inner，相当于inner捕获了outer的局部变量，即使outer早就执行完了，也可以继续使用那些变量
#### nonlocal
* 在闭包的上下文中，`nonlocal`关键字允许内部函数修改外部函数的局部变量，以实现状态的更新或持久化。
## 数据结构
### 元组
* 特点：有序；不可变；可包含不同类型元素
* 创建：使用括号加逗号，括号可省略
### 列表
* 即list
* 当创建列表是，实际上是实例化了list这个类
* 方法
	1.`append`:将单个元素添加到列表末尾
	2.`extend`:将另一个可迭代对象的所有元素添加到列表末尾
	3.`insert`:list.insert(index, element)
## 语法
### 推导式语法
#### 列表推导式
* 用于生成新的列表
* `[表达式 for 变量 in 可迭代对象 if 条件]`
```python
# 生成一个包含1到10之间所有偶数的列表 
evens = [x for x in range(1, 11) if x % 2 == 0]
```
#### 集合推导式
* 用于生成集合，使用{}
* `{表达式 for 变量 in 可迭代对象 if 条件}`
```python
# 生成一个包含1到10之间所有偶数的集合 
evens_set = {x for x in range(1, 11) if x % 2 == 0}
```
#### 字典推导式
* 用于生成字典
* `{键表达式: 值表达式 for 变量 in 可迭代对象 if 条件}`
```python
# 生成一个字典，键是1到5，值是键的平方 
squares = {x: x**2 for x in range(1, 6)}
```
#### 生成器表达式
* 返回生成器对象，使用小括号()
```python
# 创建一个生成器对象，生成1到10之间所有偶数 

evens_gen = (x for x in range(1, 11) if x % 2 == 0)
```
## 编程范式
### 消息传递模式
#### 是什么
* 是一种组织代码的方式，其中对象（函数）通过接收和处理消息来进行交互和执行操作
#### 特点
* 对象或函数封装了数据和行为