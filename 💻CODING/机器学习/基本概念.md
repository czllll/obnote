#ml 
# 基本术语
* dataset
	* instance/sample -> feature vector
	* attribute/feature : attribute value
	* attribute space/sample space
* learning/training
* example = sample + label
* classification:离散值
* regression:连续值
* supervised learning
	* classification and regression
* unsupervised learning
	* clustering
* IID, independently and identically distributed
	* independently：the outcome of one variable does not affect the outcome of other
	* identically distributed: all random variables follow the same pro
# Machine learning algorithms
* supervised learning (used most)
* unsupervised learning
* recommender systems
* reinforcement learning
## supervised learning
* input x -> output y
* learn from data labeled with the right answer
### type
#### classification
* predict categories
* small numbers of possible outputs
#### regression
* predict a number
* infinitely many possible outputs

## unsupervised learning
* find something interesting in unlabeled data
* data only comes with inputs x, but not output labels y
### type
#### clustering
* group similar data points together
	#### anomaly detection
* find unusual data points 
#### dimensionality  reduction(降维)

