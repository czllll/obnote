链接：[2779.Maximum Beauty of an Array After Applying Operation - LeetCode](https://leetcode.com/problems/maximum-beauty-of-an-array-after-applying-operation/description/)

## 题目

You are given a **0-indexed** array `nums` and a **non-negative** integer `k`.

In one operation, you can do the following:

- Choose an index `i` that **hasn't been chosen before** from the range `[0, nums.length - 1]`.
- Replace `nums[i]` with any integer from the range `[nums[i] - k, nums[i] + k]`.

The **beauty** of the array is the length of the longest subsequence consisting of equal elements.

Return *the **maximum** possible beauty of the array* `nums` *after applying the operation any number of times.*

**Note** that you can apply the operation to each index **only once**.

A **subsequence** of an array is a new array generated from the original array by deleting some elements (possibly none) without changing the order of the remaining elements.

 

**Example 1:**

```
Input: nums = [4,6,1,2], k = 2
Output: 3
Explanation: In this example, we apply the following operations:
- Choose index 1, replace it with 4 (from range [4,8]), nums = [4,4,1,2].
- Choose index 3, replace it with 4 (from range [0,4]), nums = [4,4,1,4].
After the applied operations, the beauty of the array nums is 3 (subsequence consisting of indices 0, 1, and 3).
It can be proven that 3 is the maximum possible length we can achieve.
```



## 题解

每个数可以替换为`[nums[i] - k, nums[i] + k]`这样的数即意味着每个数对应一个这样的闭区间，要求相同数字的最长子序列即是，求nums数组每个数对应闭区间重叠最多的次数。

为了方便计算我们可以将数组先排序，我们可以先维护第一个数字的区间`[x - k, x + k]`，在通过遍历其他数字区间`[y - k, y + k]`，来判断是否有重合，有重合即说明这两个数字可以变成同一个数字，即判断 `x + k >= y - k`,即`y - x <= 2k`；若没有重合则`x+k`即可向右移动；因此我们就可以使用滑动窗口，left即维持`x + k`,right维持`y - k`,先保持left不变，right为nums遍历的index,每遍历到一个数字时判断`y - x <= 2k`是否不成立,即`y - x > 2k`，若不成立则向右滑动left直到成立为止。每次判断后更新结果，结果即为当前[left,right]的长度即`right - left + 1`与先前结果的较大值。



```python
class Solution:
    def maximumBeauty(self, nums: List[int], k: int) -> int:
        nums.sort()
        res = 0
        left = 0
        for right, num in enumerate(nums): 
            while num - nums[left]  > 2 * k:
                left += 1
            res = max(right - left + 1, res)
        return res
```





